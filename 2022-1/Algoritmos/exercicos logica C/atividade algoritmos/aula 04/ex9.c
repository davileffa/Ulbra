#include <stdio.h>

int main() {
  float base = 10;
  float altura = 20;
  float area = (base * altura) / 2;
  printf("A area de um triângulo com base de %.1fm e altura de %.1f é igual a: %.1f\n", base, altura, area);
}